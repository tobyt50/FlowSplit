{
    "name": "@flowsplit/ai-service",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "uvicorn main:app --reload --port 3107 --env-file ../../../.env",
        "start": "uvicorn main:app --port 3107 --env-file ../../../.env",
        "install-deps": "pip install -r requirements.txt"
    }
}